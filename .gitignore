node_modules/
.env
.env.*
!.env.example
.next/
out/
dist/
build/
.turbo/
.mastra/
coverage/
*.log
.DS_Store
data/*.duckdb
playwright-report/
test-results/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

.vercel

# beads local read-cache — regenerated by bd, must not be tracked (cp-uif3.1)
.beads/issues.jsonl

# beads backup bookkeeping — machine-local, and records an absolute backup path
.beads/dolt-backup.json
.beads/dolt-backup-state.json

# SQLite sidecars for the gitignored *.db files
*.db-shm
*.db-wal

# Local README preview output (generated by scripts/readme-preview.mjs)
readme-preview.html

# Internal build notes kept local: they document a sibling project
# (template-mastra-base) and carry absolute developer paths.
packages/server/SPEC/
